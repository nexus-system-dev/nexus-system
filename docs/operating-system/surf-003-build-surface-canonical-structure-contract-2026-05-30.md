# SURF-003 Build Surface Canonical Structure Contract

Date: `2026-05-30`
Status: `canonical build surface contract`

## Mission

`SURF-003` turns the `SURF-001` paired workspace law into the concrete Build surface.

Build is not a Loop dashboard, not an orchestration timeline, not a proof panel, and not a chat-only screen.

```txt
Build surface = right-side persistent Nexus agent rail + live product/build canvas.
```

The old `loop` route may remain as a compatibility route name, but the visible product surface is `Build`.

## Required Regions

- `agent-conversation-rail`
- `live-artifact-build-canvas`
- `human-progress-state`
- `change-direction-affordance`
- `release-readiness-affordance`
- `continuity-restore-anchor`

## Preserved Engine Truth

- Product Graph / artifact truth remains the source of build state.
- Project Discovery Agent transcript and understanding continue into the rail.
- Product Skeleton Agent output is the first input that the Build surface renders.
- Build / Loop Agent will own continuation after `BLD-AGT-001`.
- Existing `loop` route compatibility can remain while the visible surface contract is Build.

## Removed Visible Behavior

- visible `Loop` as the product label
- orchestration-first stepper/timeline/proof-dashboard layout
- detached details panel
- internal route/status language
- canvas that is only a static preview with no agent rail
- chat rail that cannot accept input

## Built Behavior

- right-side Nexus navigation bar
- right-side persistent writable agent conversation rail
- live build canvas as dominant region
- compact build truth anchors for progress, change, release, and continuity
- DOM contract markers for `SURF-003`

## Closure Standard

`SURF-003` can close only when:

- the active Build/Loop screen exposes `data-build-surface-contract="SURF-003"`
- the same screen still exposes the `SURF-001` paired workspace law
- the agent rail is visible, persistent, and writable
- the live build canvas is visible and primary
- tests prevent old Loop-only/orchestration-first structures from returning
- live browser proof confirms the Build surface contract and both core regions
- the implementation task map is updated with truthful closure evidence
