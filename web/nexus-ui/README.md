## Nexus UI

This directory contains the new Nexus frontend layer derived from the approved
Figma Make export. It is intentionally isolated from the current `web/`
runtime until each loop screen is wired to real Nexus runtime data.
